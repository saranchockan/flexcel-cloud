import React, { Component } from 'react'
import PropTypes from 'prop-types'
import "./FileManager.css";

const FileManager = (props) => {

    return (
        <div className="FileManager_title">
			<p>File Manager</p>
        </div>
	);
}

export default FileManager;