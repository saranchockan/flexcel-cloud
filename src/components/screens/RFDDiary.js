import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class RFDDiary extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                
            </div>
        )
    }
}